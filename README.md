# License-Manager-dotnet
A user-based License Manager with capabilities to perform CRUD operations to a MSSQL Server database, built in .NET Core and Angular. Uses singleton approach for object creating to make a scalable project.
